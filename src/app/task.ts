import { DeclarationListEmitMode } from "@angular/compiler";

export class Task {
    
        TaskId: Int16Array;
        QuoteType: string;
        QuoteNumber: Int16Array;
        Contact: string;
        TaskContent: string;
        DueDate: Date;
        TaskType: String;
    
}
